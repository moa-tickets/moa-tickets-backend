package stack.moaticket.domain.booking_history.repository;

import org.springframework.stereotype.Repository;

@Repository
public interface BookingHistoryRepository {
    // intentionally empty (QueryDSL only)
}

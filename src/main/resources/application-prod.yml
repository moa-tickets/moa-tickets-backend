spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    org.hibernate.SQL: debug

app:
  frontend:
    url: https://moatickets.dev
  ai:
    url: ${AI_URL}